import React from 'react';
import styles from './Footer.module.css';

const { mainFooter } = styles;

const footer = () => {
    return (
        <footer className={mainFooter}>

        </footer>
    );
}

export default footer;